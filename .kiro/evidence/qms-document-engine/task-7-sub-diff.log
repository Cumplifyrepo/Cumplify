# Task 7 — Subscription + diff
# Evidence log (rule 7/8: evidence + checkbox in same commit)
# Timestamp: 2026-07-15T16:40:00Z
# Base commit: f546a9b (Task 10 fix accepted)

## Gates executed

| Gate | Command | Exit code | Result |
|------|---------|-----------|--------|
| TypeScript | `npx tsc --noEmit` | 0 | Clean |
| Unit tests | `npx vitest run` | 0 | 804 tests pass, 70 files |

## Fix round (rejection 906833a → this commit)

| Finding | Fix | Test pinned |
|---------|-----|-------------|
| LCS on object reference (v1-vs-v1 → 45/45 live) | LCS on sentence.text (extractSentenceTexts maps {text,factRefs} → text) | Fixtures use real {text, factRefs} shape; identical → 0/0 |
| Non-prose sections invisible to diff | Kind comparison: gap→prose shows as kindChange with counts | gap→prose test asserts kindChange + additions |
| process.env at synth bakes '' (never reads S3 live) | ApiStackProps += generalBucketName/Arn/Key; cumplify-stage plumbs them; env from props | Structural (tsc + synth-level) |
| resources:['*'] on S3 grant | Scoped to ${generalBucketArn}/tenants/* + s3GeneralKey.grantDecrypt | Structural |
| Silent 0/0 on empty content_ref / S3 failure | Typed CONTENT_UNAVAILABLE (no fallback {sections:[]}); '' refs throw | Test: empty ref → CONTENT_UNAVAILABLE |
| No document_id match (cross-doc diff possible) | SQL += AND v1.document_id = v2.document_id; no row → VERSION_MISMATCH | Test: no row → VERSION_MISMATCH |
| Evidence claimed "subscription auth denial test" (none existed) | Corrected: coverage rides loop-level C-6 VTL assertion (schema-guard test 3 + 84-count) | — |

## Deliverables

| File | Purpose |
|------|---------|
| `infra/lib/api-stack.ts` | onGenerationProgress in subscriptionFields loop (C-6 auth) + M1 S3 read access |
| `infra/lib/api-stack.unit.test.ts` | Resolver count 83→84 |
| `services/api/src/resolvers/m1.ts` | Real getDocumentVersionDiff: S3 load + section alignment + LCS |
| `services/api/__tests__/resolvers/m1-diff.test.ts` | 6 hermetic diff tests |

## Implementation

**onGenerationProgress subscription:**
- Added to `subscriptionFields` array (loop-generated with None-DS + C-6 VTL template)
- Same pattern as onDocumentStatusChanged: checks `$ctx.identity.resolverContext.tenantId != $ctx.args.tenantId` → $util.unauthorized()
- SDL field already exists from Task 3 (not re-appended)

**getDocumentVersionDiff (design §3 — CLOSES standing BLOCKED diff item):**
1. Fetch `content_ref` from both versions (`m1.document_versions`, ::uuid casts)
2. Load content JSONs from S3 via `CONTENT_BUCKET` env (graceful degradation if unconfigured)
3. Align sections by `harmonizationKey` (bare key = shared; key#standard = forked/standard_only)
4. Sentence-level LCS diff → existing Diff type (additions, deletions, content as JSON diff summary)

**Diff algorithm:**
- Added section: all sentences count as additions
- Removed section: all sentences count as deletions
- Changed section: LCS identifies common subsequence; non-LCS sentences in v1 = deletions, in v2 = additions
- Identical section: no entry in diff (clean output)

**Tests (6 hermetic, S3 mocked):**
1. SQL uses ::uuid casts + real content_ref column name
2. Identical → 0/0
3. Added section → sentences counted
4. Removed section → sentences counted
5. Changed sentences → LCS-based add/del
6. harmonizationKey#standard convention honored (forked sections align correctly)

## Deferred

**regenerateSection (GEN-6):** explicitly deferred — depends on generation SFN (architect Task 5/6 scope). Reported here per standing rule; not coded around.

## Note

This CLOSES the standing frontend-app BLOCKED diff item (same precedent as listFormRecords closing listRecords in spec 41 Task 3).

## D-rung

D2 — hermetic tests pass. Live execution requires CONTENT_BUCKET env to be plumbed (deploy-time prop).

## Round history (architect validation)

- **Round 1 (906833a): REJECTED.** (1) LCS compared sentence elements with
  `===` — production sentences are `{text, factRefs}` OBJECTS (derive.ts
  spreads compose-section output verbatim); demonstrated against the LIVE
  manual content JSON: same version diffed vs itself → 45 additions /
  45 deletions (expected 0/0). Fixtures pinned plain strings — an invented
  shape. (2) CONTENT_BUCKET read from process.env at SYNTH time → ''
  baked into the Lambda; loadContentJson degraded silently → every live
  diff 0/0 (facade as deployed). (3) s3:GetObject on resources:['*'] with
  "when ready" comment — coded-around + missing kms:Decrypt on the SSE-KMS
  bucket. (4) Load failure / empty ref returned 0/0 — indistinguishable
  from "identical". (5) SQL allowed cross-document version pairs.
  (6) "subscription auth denial" checkbox claimed a test that was not added.
  Architect contributing miss (owned): the Task 7 go never pinned the
  sentence element shape.
- **Round 2 (28df9e9): ACCEPTED.** LCS on sentence.text via
  extractSentenceTexts; kind transitions (gap→prose) reported as
  kindChange; ApiStackProps += generalBucketName/Arn/s3GeneralKey plumbed
  from DataStack/SecurityStack exports (AiStack pattern); grant scoped to
  `${generalBucketArn}/tenants/*` + grantDecrypt; typed CONTENT_UNAVAILABLE
  / VERSION_MISMATCH (no silent fallback); fixtures use the real
  {text, factRefs} shape. Gates re-executed by architect: 804 backend +
  85 frontend, tsc 0.

## Architect live readback (2026-07-15, deploy + behavior)

Deploy: `cdk deploy CumplifyPipeline/Dev/ApiStack` 21:50–21:51 UTC, exit 0,
47.2s — SubonGenerationProgress resolver CREATED, ResolverM1Fn policy+env
updated.

| # | UTC | Step | Exit | Outputs SHA (first 20) | Result |
|---|-----|------|------|------------------------|--------|
| 0 | 21:32 | OFFLINE: fixed diff fns (m1.ts:348-472 verbatim, esbuild type-strip) vs LIVE manual v1.json ×2 | 0 | input 97b9216620fcdb1fd3cd | same-version → **0/0** (round-1 code: 45/45); synthetic 1-sentence mutation + 1 section removed → 1 add / 2 del |
| 1 | 21:51 | ResolverM1Fn env CONTENT_BUCKET | 0 | — | `dev-datastack-generalbucketad391c6e-lbcsvfej9hap` (real bucket, from props — not synth-env) |
| 2 | 21:52 | LIVE getDocumentVersionDiff manual v1 (49adf9ed…) vs itself | 0 | 0cbce00037b0236b7dec | `{"additions":0,"deletions":0,"content":"{}"}` — full pipeline: RLS SQL → scoped S3 GetObject → KMS decrypt → parse → diff |
| 3 | 21:52 | LIVE diff manual version vs matrix version (cross-document) | 0 (typed error) | 9e3195cfeac5f1a336e2 | `VERSION_MISMATCH` thrown — same-document constraint live |
| 4 | 21:52 | appsync get-resolver Subscription/onGenerationProgress | 0 | — | NoneDataSource; request VTL = `#if($ctx.identity.resolverContext.tenantId != $ctx.args.tenantId) $util.unauthorized()` — C-6 live |

Manual v1.json fetched fresh for probe 0 hashes 97b9216620fcdb1fd3cd — byte-identical
to the Task 6 readback sha (S3 content stable since finalize).

## D-rung & carries

- getDocumentVersionDiff: **D3** (deployed + live-verified above).
  onGenerationProgress: resolver live with C-6 VTL; publisher live since
  Task 5 (~37 IAM mutations). End-to-end WebSocket fan-out delivery remains
  for Task 11 / ACC-6 (needs a ws client) — carried, not claimed.
- CARRY-FORWARD (accepted, reported): same-kind gap/naJustification CONTENT
  changes are not compared (kind transitions are). Live gap content is
  `{missingSources:[...]}` — a same-kind source-list change between versions
  will not surface in the diff. Surface is empty today (no document has a
  v2 until regenerateSection ships); folds into the regenerateSection wave
  together with fixing the inert `gap?: string` type annotation
  (actual shape: object).
- Live real-content diff (non-zero) was proven OFFLINE against live bytes
  (probe 0); a live non-zero probe needs a second version of the same
  document — arrives with regenerateSection.
