# B2 amendment 1 (65e2ff4) — REJECTED on live dry-run (SQLState 23505 at Step 1)

**Date:** 2026-07-23 · **Validator:** architect · **Method:** the migration's
exact statements executed in a single Data API transaction on the LIVE dev
cluster as master, then ROLLED BACK (zero trace). 19:15:44Z. This dry-run
harness (`scratchpad/dryrun_migration_019.py` pattern) is now the
validation gate for any DML migration.

## New fatal defect — B2A-ORDER-1 (reproduced, not theoretical)

`CREATE UNIQUE INDEX idx_m1_documents_tenant_hk` runs BEFORE the dedup.
Step 1 then stamps identical derived keys onto still-existing duplicates →
**`duplicate key value violates unique constraint` (23505) on the very
first backfill statement.** Required order: derive keys → dedup (2a/2b/2c)
→ CREATE UNIQUE INDEX last.

## Still-open defects from the review

- **B2A-TXN-1:** 019 is the ONLY migration containing `BEGIN;`/`COMMIT;`.
  The runner wraps every migration in its own Data API transaction; the
  inner `COMMIT` commits that transaction EARLY, the runner's
  CommitTransaction then fails → deploy aborts AFTER changes landed, with
  `_migrations` bookkeeping in an inconsistent state. Remove both — the
  runner owns the transaction.
- **B2A-SCOPE-1:** policy (60 rows/3 titles) and scope (32/3) remain
  outside Step 1 — they stay NULL-keyed and duplicate-inflated. They are
  NOT produced by finalize-manual (grep is empty) — the amendment must
  name their actual producer and either fold them into the dedup or defer
  with an explicit owner-visible cost ("register keeps 84 duplicate
  policy/scope rows").

## Validated CORRECT by the same dry-run (keep, do not rework)

- RLS bracket executes as designed (NO FORCE/FORCE both accepted in-txn).
- Disposition heuristic string is EXACT on live data: 1248/1252 versions
  are `'Generated by DocGen run'`; the 4 others (`Section edit: 5.1`,
  `Section 4.4 regenerated` ×2, `…entries refreshed`) are precisely the
  human/regen versions the re-parent branch must preserve.
- `document_versions` has NO `(document_id, version_no)` unique constraint
  (pkey + tenant index only) → re-parenting cannot abort on conflict.
- Title-regex derivation and DDL are syntactically sound.

## Note

Steps 2a/2b/2c and the post-state were NOT exercised (blocked at Step 1).
After the ordering fix, the full dry-run must pass end-to-end — including
post-checks: 0 orphan versions, 0 duplicate (document_id, version_no)
pairs, expected per-type counts — before the stage re-validates.
