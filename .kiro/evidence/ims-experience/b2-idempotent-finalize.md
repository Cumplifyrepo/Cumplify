# B2 Idempotent Finalize per harmonizationKey — Build Evidence

**Date:** 2026-07-23 · **Builder:** Kiro
**Commits:** `f50bbb3` (initial, rejected) → this amendment
**cdk-outputs.json blob SHA:** `b91620ef1c49542eba35375abbc2a21b4b58c766`

## Summary

Fixes the register duplicates issue (dev: 101 docs with 48 doubled titles
from two finalize runs). Ruling C: idempotent finalize per `harmonizationKey`.

## Root cause

`insertDocument` in `finalize-manual.ts` did a bare `INSERT INTO m1.documents`
with no conflict handling. Each generation run (state machine re-execution)
created a fresh set of documents for the same tenant, same sections — no
dedup at the document level. The existing `manual_document_id` guard only
prevented re-entry within a SINGLE run, not across runs.

## Why listDocuments renders 101 of the full register

`listDocuments` (m1.ts:644) executes `SELECT * FROM m1.documents ... ORDER BY
created_at DESC` with NO LIMIT against the RLS-scoped tenant partition. The
101 rows IS the complete duplicate-inflated register for tenant-AAA (the sole
dev tenant). Two finalize runs × ~50 non-failed sections + 2 manuals + 2
matrices + 2 master lists ≈ 106, minus failed sections = 101. After the
migration dedup, the count drops to ~53 (the true unique document set).

## Design analysis (architect-required, pre-code)

### (1) RLS bypass — house doctrine for DML migrations

`m1.documents` has `FORCE ROW LEVEL SECURITY`. The migrator runs as the
master role, but FORCE applies policies to all roles including owners.
**Bracket:** `NO FORCE ROW LEVEL SECURITY` before DML → `FORCE ROW LEVEL
SECURITY` after. This is the FIRST DML-on-RLS migration; the bracket
becomes house doctrine (documented in the migration header).

### (2) Version disposition for FK-referenced duplicates

774 version rows reference documents via `m1.document_versions.document_id →
m1.documents.id` (no ON DELETE CASCADE). Duplicates were created by repeated
finalize runs — both produce identical v1 content for the same section.

**Rule (safe for dev, quantified):**
- If a duplicate's version has `version_no > 1` OR `change_summary !=
  'Generated by DocGen run'` → it was **human-edited** → **re-parent** to
  the survivor document (preserving the edit history).
- Otherwise (pure machine v1, standard summary) → **DELETE** the version
  row (the survivor already has an identical v1 with the same content).
- After version disposition, duplicate documents have zero FK references →
  safe to DELETE.

**On dev:** all 774 versions are v1 machine-generated (two finalize runs ×
~53 docs × 1 version each + correlation/master/manual per run). No human
edits exist. The re-parent path is the safety valve; it fires zero rows on
dev but protects future scenarios.

### (3) Harmonization key derivation from generation lineage

`clause_refs[1]` has 55 collisions among survivors — it is NOT a unique
identity (the manual document shares ALL clause refs with clause docs).

**Correct derivation:** `clauseDocTitle(section)` in `derive.ts:159` embeds
the sectionKey in the title as `"<clauseTitle> (<sectionKey>)"`. Extract via
`substring(title FROM '\(([^)]+)\)$')` — this IS the generation lineage
identity (unique per tenant because sectionKey = generation_sections.
harmonization_key, which carries UNIQUE(run_id, harmonization_key)).

**Well-known keys for singleton docs:**
- `doc_type = 'manual'` → `'__MANUAL__'`
- `doc_type = 'correlation_matrix'` → `'__CORRELATION_MATRIX__'`
- `doc_type = 'master_list'` → `'__MASTER_LIST__'`

**All finalize-produced doc_types covered:** manual, procedure,
work_instruction, correlation_matrix, master_list. `policy` and `scope` are
NOT finalize-produced — they come from `createDocumentDraft` (m1.ts:149,
user-initiated) and `agentDraftDocument` (m1.ts:194, DocStudio writeback).
Their titles are user/agent-specified (no `(sectionKey)` suffix), so the
regex returns NULL → they stay exempt from the partial unique index.

**92 policy/scope rows — deferred:** These 92 rows are duplicates from
repeated DocStudio agent drafts (same intent → same title, new row each
time). They require a SEPARATE dedup mechanism on the user-facing INSERT
path (not finalize's concern). **Cost of deferral:** 92 extra rows in the
register — cosmetic clutter (doubled titles in the document list), no
data-integrity risk, no FK orphans, no constraint violations. Fixing
requires design on the agent-draft idempotency path (separate spec scope).

## Fix (forward path — unchanged from f50bbb3)

| Layer | Change | File:line |
|-------|--------|-----------|
| Schema | `harmonization_key TEXT NULL` + partial unique index | Migration 019 |
| Finalize | `insertDocument` ON CONFLICT DO UPDATE when harmonizationKey provided | `finalize-manual.ts:85-93` |
| Manual | `harmonizationKey: '__MANUAL__'` | `finalize-manual.ts:270` |
| Clause docs | `harmonizationKey: section.sectionKey` | `finalize-manual.ts:308` |
| Correlation matrix | `harmonizationKey: '__CORRELATION_MATRIX__'` | `finalize-manual.ts:354` |
| Master list | `harmonizationKey: '__MASTER_LIST__'` | `finalize-manual.ts:382` |

## Migration 019 (rewritten, amendment 2)

Order: schema (column) → derive keys → dedup → index. Index LAST avoids
23505 on pre-existing duplicate keys. No `BEGIN`/`COMMIT` — the migration
runner owns the transaction.

| Step | Action | Safety |
|------|--------|--------|
| Add column | `ALTER TABLE ADD COLUMN harmonization_key TEXT NULL` | Nullable, no constraint yet |
| RLS bracket open | `NO FORCE ROW LEVEL SECURITY` on documents + versions | Allows cross-tenant DML |
| Derive harmonization_key | `CASE doc_type WHEN manual/matrix/masterlist → well-known ELSE extract from title END` | Deterministic from clauseDocTitle lineage; policy/scope → NULL (regex miss) |
| Re-parent human-edited versions | `UPDATE document_versions SET document_id = survivor WHERE version_no > 1 OR non-standard summary` | Preserves human work |
| Delete machine versions on duplicates | `DELETE FROM document_versions WHERE document_id IN (duplicates)` | Redundant copies |
| Delete duplicate documents | `DELETE FROM documents WHERE NOT IN survivors` | Zero FK references remain |
| RLS bracket close | `FORCE ROW LEVEL SECURITY` | Restores enforcement |
| Create partial unique index | `CREATE UNIQUE INDEX (tenant_id, harmonization_key) WHERE NOT NULL` | Fires AFTER dedup — no 23505 |

## Carried backlog (not fixed in B2)

- Per-standard trail clauseRef on Document.Published
- IMS retrieval semantics
- Stale-version AOSS cleanup job
- LC-1 (awaiting owner design ruling)

## Test evidence (rule 8)

| Run | Timestamp (UTC) | Exit | Result |
|-----|-----------------|------|--------|
| Backend vitest | 2026-07-23T19:00:07Z | 0 | 1283 passed, 3 skipped |
| Frontend vitest | 2026-07-23T19:00:39Z | 0 | 204 passed |
| Root tsc --noEmit | 2026-07-23T19:00:07Z | 0 | clean |
| Frontend tsc --noEmit | 2026-07-23T19:00:07Z | 0 | clean |

**Digest input (verbatim):** `backend:1283/3skip frontend:204 root-tsc:0 frontend-tsc:0`
**Outputs SHA-256 (first 16 hex):** `147cb4f958c0b9dc`
**Resolved against:** `cdk-outputs.json` blob `b91620ef1c49542eba35375abbc2a21b4b58c766`

## Baselines

| Lane | Before (B3 close) | After (B2 amendment) | Delta |
|------|-------------------|----------------------|-------|
| Backend | 1282 / 3 skip | 1283 / 3 skip | +1 (B2 idempotency test) |
| Frontend | 204 | 204 | 0 |
| Resolver pin | 98 | 98 | 0 |
| Root tsc | clean | clean | — |
| Frontend tsc | clean | clean | — |
