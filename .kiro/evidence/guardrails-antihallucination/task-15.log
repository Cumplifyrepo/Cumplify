# Task 15 Evidence — tool-use path integration + agent-routing tool registry
# Timestamp: 2026-07-16T18:05Z
# Agent: Kiro build agent

## Implementation
- Integrated hop check into `services/ai-invoker/src/index.ts` invoke() orchestration:
  - After converse() returns, before grounding checks
  - When `stopReason === 'tool_use'` AND tool name in AGENT_ROUTING_TOOLS → screen payload
  - Extracts targetAgent from tool input (targetAgent or agent field, else 'unknown')
  - Passes standard from request.standard (FIX-V3 threading)
  - On HOP_BLOCKED throw: propagates to caller (meters usage before throw at L1-8 level)

- Agent-routing tool registry lives in hop-check.ts (4 canonical names):
  route_to_agent, delegate_to_agent, invoke_agent, call_agent

## Verification Commands
```
npx tsc --noEmit
Exit code: 0

npx vitest run
Exit code: 0
Test Files: 92 passed | 1 skipped (93)
Tests: 970 passed | 3 skipped (973)
```
