# Task 3.1 Closure Report — Pipeline Adoption

**Spec:** platform-foundation
**Task:** 3.1 (GitHub remote + CodeStar Connection + pipeline deploy)
**D-rung:** D3 (pipeline deployed to mgmt + first run no-op verified)
**Date:** 2026-07-04
**Witness:** Architect (human)

---

## Acceptance

Pipeline execution `6aec0e7a-561c-4008-b4fa-012d1345e877` completed
2026-07-04 ~01:58Z-02:08Z. All stages through Dev green; Staging
ManualApproval left pending (intentional — AZ + cost gates unmet).

Forensic CloudFormation stack-event audit (02:00-02:15Z, dev 697114252993):
- Dev-NetworkStack: CDKMetadata only → functional no-op
- Dev-SecurityStack: CDKMetadata only → functional no-op
- Dev-IdentityStack: CDKMetadata only → functional no-op
- Dev-DataStack: CDKMetadata + AuroraClusterRotationSingleUser nested-stack
  TemplateURL (presigned SAR URL of the SAME rotation app v1.1.671; zero
  resource changes beneath) → functional no-op

**Verdict: ZERO functional resource changes across all 4 stacks.**

---

## Known Framework Artifacts (expected on every pipeline run)

1. `AWS::CDK::Metadata` analytics string differs between local and pipeline
   synth — cosmetic, not drift.
2. DataStack Aurora rotation nested-stack presigned TemplateURL regenerates
   every synth (serverlessrepo changeset copy of the same app version).
   Not drift. Zero resource changes beneath.

---

## Path to Green (failure record)

| # | Stage | Failure | Resolution |
|---|---|---|---|
| 1–4 | Source | "[GitHub] No Branch [develop] found for FullRepositoryName [strivanallc-crypto/Cumplify]" | Pipeline configured for prior account's repo post re-homing to Cumplifyrepo. Owner-ordered unwiring (7aa30b1) + new connection under Cumplifyrepo (42bc69e). |
| 5 | Build | cfd8a62d: "npm run test, exit status 1" — CodeBuild standard:7.0 ships Node 18.20.8; toolchain requires >=20 (vitest 4, aws-cdk-lib 2.261) | Fixed 430a901: standard:8.0 (Node 22), image verified via list-curated-environment-images. |
| 6 | All | ✅ SUCCESS — execution 6aec0e7a | — |

**Connection lesson:** CodeStar connections bind to a GitHub App installation
at handshake time. A deleted-and-recreated repo (new repo ID) orphans any
connection scoped to the old one. Replacement via fresh OAuth handshake was
required — rescoping is not supported.

---

## Carried Condition (blocks staging/prod gate approval)

**Staging/prod availabilityZones NOT YET POPULATED** in `env-config.ts`.
The `describe-vpc-endpoint-services` intersection query must be run per
target account before the pipeline's ManualApproval gates can be approved.
Owner standing-cost approval also required (~$96/mo staging, ~$250/mo prod).

This sub-item remains unchecked in tasks.md. Task 3.1 closes at D3 for the
dev-facing acceptance (pipeline deployed + first run no-op) with this explicit
carried condition documented.

---

## Evidence

- `.kiro/evidence/platform-foundation/3.1-pipeline-noop-architect-witness.log`
  (architect-authored, execution 6aec0e7a, commit 09d19ca)

---

## Carried Condition — RESOLVED (2026-07-04, post-closure amendment)

The staging/prod availabilityZones sub-item is now complete: architect ran the
five-service describe-vpc-endpoint-services intersection inside both target
accounts (assume-role). Both intersect to [us-east-1b, us-east-1c, us-east-1d]
(AOSS constraining, same as dev); picked ['us-east-1b','us-east-1c'] per env.
env-config.ts populated; synth verified (Staging/Prod NetworkStack templates pin
b/c, Fn::GetAZs fallback absent; Dev template unchanged).
Evidence: 3.1-az-intersection.log. Remaining gate blocker: owner cost approval
only (~$96/mo staging, ~$250/mo prod).
