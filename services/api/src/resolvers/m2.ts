/**
 * M2 CAPA resolver — placeholder for Task 10 implementation.
 * INVARIANT (C-2): set_config('app.tenant_id', :tenantId, true) MUST be the
 * FIRST statement in every BeginTransaction. Never false. Never bare ExecuteStatement.
 */
export async function handler(_event: unknown): Promise<unknown> {
  throw new Error('M1 resolver not yet implemented (Task 10)');
}
